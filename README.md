# OpenMail
MailServer + Roundcube / SnappyMail

## Usage

Run with Roundcube:

```bash
./setup.sh --roundcube
```

Run with SnappyMail:

```bash
./setup.sh --snappymail
```

Run just the mailserver:
```bash
./setup.sh
```
